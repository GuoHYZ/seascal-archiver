# === 文档转换（必装） ===
python-pptx>=0.6.21
python-docx>=0.8.11
openpyxl>=3.1.0
pywin32>=305          # 旧格式转换 (.doc/.ppt/.xls)，需本机 Microsoft Office

# === RAG 语义检索 ===
faiss-cpu>=1.7.0
sentence-transformers>=3.0.0
numpy>=1.21

# === LLM 在线回答 ===
openai>=1.0.0         # search_rag.py --llm 模式

# === BM25 混合检索 ===
# 自实现于 rag_utils.py，无额外依赖